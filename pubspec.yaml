name: genro_app
description: "Projeto feito para colaborar com o engajamento das lendas"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^2.26.0
  firebase_auth: ^4.17.8
  firebase_ui_auth: ^1.13.1
  cupertino_icons: ^1.0.6
  provider: ^6.1.2
  go_router: ^13.2.0
  get_it: ^7.6.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
flutter:
  uses-material-design: true